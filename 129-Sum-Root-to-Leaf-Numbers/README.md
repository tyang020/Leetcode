We use recursion method to solve this problem.

When using iterative method, we have to store both current sum and current node to stack. Otherwise, we have to modify node values, which is not recommended.